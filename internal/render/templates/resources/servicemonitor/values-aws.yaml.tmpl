metrics:
  serviceMonitor:
    # -- Label the in-cluster Prometheus Operator selects on. Amazon Managed
    # Service for Prometheus does not read ServiceMonitors at all — it is a
    # remote-write endpoint, and the AWS managed collector has its own scrape
    # config. Match this to your operator's release name.
    additionalLabels:
      release: prometheus
