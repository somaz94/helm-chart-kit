metrics:
  serviceMonitor:
    # -- Google Managed Prometheus reads PodMonitoring, not ServiceMonitor.
    # Enable this only on a cluster running its own Prometheus Operator.
    additionalLabels:
      release: prometheus
