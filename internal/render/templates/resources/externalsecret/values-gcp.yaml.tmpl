externalSecret:
  secretStoreRef:
    # -- SecretStore pointing at Google Secret Manager. The store is cluster
    # infrastructure and is not created by this chart.
    name: gcp-secretmanager
    kind: ClusterSecretStore
