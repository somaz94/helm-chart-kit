certificate:
  issuerRef:
    # -- ClusterIssuer whose DNS-01 solver is route53. A wildcard or a
    # private-DNS name cannot be proven over HTTP-01, so DNS-01 is the one
    # that works here.
    name: letsencrypt-route53
    kind: ClusterIssuer
