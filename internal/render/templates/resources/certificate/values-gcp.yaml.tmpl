certificate:
  issuerRef:
    # -- ClusterIssuer whose DNS-01 solver is clouddns. A wildcard or a
    # private-DNS name cannot be proven over HTTP-01, so DNS-01 is the one
    # that works here.
    name: letsencrypt-clouddns
    kind: ClusterIssuer
